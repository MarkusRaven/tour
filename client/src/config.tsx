import { DefaultTheme } from "styled-components"

const theme: DefaultTheme = {
  colors: {
    main: {
      main: '#000',
      contrastText: '#fff',
      hover: '#242424'
    },
    primary: {
      main: '#3f50b5',
      contrastText: '#fff',
      hover: '#6f7ed9'
    }
  }
}

export const config = {
  theme
}
